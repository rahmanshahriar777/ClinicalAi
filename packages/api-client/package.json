{
  "name": "@app/api-client",
  "version": "1.0.0",
  "private": true,
  "description": "Typed HTTP client for the Clinical AI Platform API (web + mobile)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run"
  },
  "dependencies": {
    "@app/shared": "workspace:*"
  },
  "devDependencies": {
    "@app/config": "workspace:*",
    "eslint": "^8.57.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
