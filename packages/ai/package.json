{
  "name": "@app/ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI orchestration: gateway pipeline, providers, prompts, PHI redaction, safety rules, evaluators",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@app/shared": "workspace:*",
    "@aws-sdk/client-bedrock-runtime": "^3.670.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@app/config": "workspace:*",
    "@types/node": "^20.16.10",
    "eslint": "^8.57.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  }
}
